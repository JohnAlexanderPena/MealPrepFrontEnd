const initialState = {
  meals: [],
  loggedIn: false,
  username: "",
  packages: [],
}


const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
        return state
  }
}


export default reducer
